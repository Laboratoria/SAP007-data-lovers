import { example } from './data.js';
import data from './data/athletes/athletes.js';
console.log(example, data);


/*const cartasPaíses = data.athletes.map((país)=>{
  return `<div><p class="conteudo-países">${país.team}
  </p> <p class="conteudo-países">${país.name}</p></div>`})

console.log (cartasPaíses);
document.getElementById("cards-países").innerHTML = cartasPaíses;*/


